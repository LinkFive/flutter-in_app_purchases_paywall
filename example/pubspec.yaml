name: example
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_countdown_timer: ^4.1.0
  in_app_purchases_paywall_ui: ^1.0.0
  in_app_purchases_intl: ^1.0.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  cupertino_icons: ^1.0.5
  responsive_spacing: ^1.1.0

dependency_overrides:
  in_app_purchases_paywall_ui:
    path: ../packages/in_app_purchases_paywall_ui

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
